Check out the sample to see how to use the API. Fairly easy.

Using the ROM Manager Scripting API requires ROM Manager premium.
Installing a single zip (to make your ROM do an OTA update for example)
can be used by both free and premium users.